# Website images

Place the original, unmodified website image files supplied by the project owner in this directory.

Do not pre-crop, resize, stretch, recompress, rename, or replace source files. Reproduction crops belong in CSS through an explicit `Media` container, `object-fit: cover`, and a documented focal position. The default image behavior is `contain`.
