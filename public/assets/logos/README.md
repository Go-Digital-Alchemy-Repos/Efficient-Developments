# Logos

Place the original logo exports supplied by the project owner here.

The shared `Logo` component currently expects:

- `efficient-developments.svg`
- `efficient-developments-inverse.svg` (only if the source design requires an inverse mark)

Update the component paths to match the exact supplied filenames rather than renaming or recreating the source files.
